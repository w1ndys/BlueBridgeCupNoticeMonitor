[project]
name = "lanqiao-monitor"
version = "1.0.0"
description = "蓝桥杯通知监控系统"
requires-python = ">=3.10"
dependencies = ["requests>=2.28"]

[project.scripts]
lanqiao-monitor = "main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
